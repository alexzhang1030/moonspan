// M0-03h3 agreement emitter executable (Wasm).
pkgtype(kind: "executable")

supported_targets = "wasm"

import {
  "moonspan/rclmbt/protocol",
  "moonbitlang/core/encoding/utf8",
  "moonbitlang/core/json",
}

formatter(ignore: [ "jobs.mbt" ])
