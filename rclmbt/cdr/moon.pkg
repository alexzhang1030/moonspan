// CDR core package (M1-01). Bounded CDR1 reader and writer surfaces.
// Production dependency surface uses core/prelude, core buffer, and utf8.

supported_targets = "wasm"

import {
  "moonbitlang/core/buffer",
  "moonbitlang/core/encoding/utf8",
}
