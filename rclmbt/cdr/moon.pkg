// CDR core package (M1-01b). Bounded CDR1 reader and writer surfaces.
// Production dependency surface uses core/prelude and core buffer.

supported_targets = "wasm"

import {
  "moonbitlang/core/buffer",
}
