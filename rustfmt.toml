# rustfmt 1.97 (edition 2024). Same indent as vue-vet.
# Vendored bindgen output is rustfmt::skip (see generate-rcl-bindings.sh).
style_edition = "2024"
tab_spaces = 2
use_small_heuristics = "Max"
use_field_init_shorthand = true
reorder_modules = true
